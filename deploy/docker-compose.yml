services:
  rn-3303-web:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: rn-3303-web
    restart: unless-stopped
    ports:
      - "3303:8080"
    environment:
      NODE_ENV: production
      PORT: 8080
